import { NumericDirective } from './only-numbers.directive';

describe('NumericDirective', () => {
  it('should create an instance', () => {
    //const directive = new NumericDirective();
    //expect(directive).toBeTruthy();
  });
});
